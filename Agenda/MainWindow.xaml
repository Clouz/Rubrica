<Window x:Class="Agenda.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Agenda" Height="600" Width="500">
    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="OnCloseExecuted" />
    </Window.CommandBindings>
    
    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="Close" />
    </Window.InputBindings>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="DataGrid" Grid.Row="0" Margin="5,5,5,0" AutoGenerateColumns="False" IsReadOnly="False" CanUserAddRows="True" ItemsSource="{Binding User}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" MinWidth="70" Width="auto"/>
                <DataGridTextColumn Header="Dep" Binding="{Binding Department}" MinWidth="50" Width="auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBox x:Name="SearchBar" Grid.Row="1" Margin="5" TextChanged="SearchBar_TextChanged" VerticalContentAlignment="Center"/>
    </Grid>
</Window>
